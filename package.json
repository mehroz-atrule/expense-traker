{
  "scripts": {
    "build:frontend": "cd Expense Tracker && npm run build",
    "build:backend": "cd backend && npm run build",
    "build-check": "cross-env npm run build:frontend && npm run build:backend"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "husky": "^9.1.7"
  }
}
